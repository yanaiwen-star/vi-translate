fastapi>=0.135.0
uvicorn[standard]>=0.42.0
websockets>=16.0
# Phase 1+ dependencies (installed so the image is ready for later phases)
sqlalchemy>=2.0.0
psycopg[binary]>=3.1.0
redis>=5.0.0
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt<4.0.0  # passlib 1.7.4 尚未适配 bcrypt 4.x / 5.x 的 __about__ 移除
python-multipart>=0.0.9
httpx>=0.27.0
python-dotenv>=1.0.0
cryptography>=42.0.0

# 阿里云短信 SDK（网页端手机号绑定验证码）
# 用新版 OpenAPI SDK（alibabacloud_dysmsapi20170525），老 SDK 包名 aliyunsdkdysmsapi
# 已不在主流 PyPI 镜像中（清华/阿里云/PyPI 官方均 404），且容器内无 git，无法从 GitHub 装。
# 新 SDK 只需一行，依赖 alibabacloud_tea_openapi / alibabacloud_credentials 自动拉取。
alibabacloud_dysmsapi20170525>=4.6.0

# 自建图形验证码（短信防刷，依赖 captcha 库）
captcha>=0.6.0

